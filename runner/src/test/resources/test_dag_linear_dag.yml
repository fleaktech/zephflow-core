jobContext:
  otherProperties:
  metricTags:
  dlqConfig:

dag:
  # a -> b(num times 2) -> c
  - id: "a"
    commandName: "testSource"
    config:
    outputs: [ "b" ]
  - id: "b"
    commandName: "eval"
    config:
      expression: |
        dict(
          num=$.num * 2
        )
    outputs: [ "c" ]
  - id: "c"
    commandName: "testSink"
    config: